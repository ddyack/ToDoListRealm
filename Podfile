project 'ToDoListRealm.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '14.0'

target 'ToDoListRealm' do
  pod 'RealmSwift', '~> 5.00', :modular_headers => true
  pod 'Realm', '~> 5.00', :modular_headers => true

end
